"use strict";
/* //normal function

function add(num1:number,num2:number=50){
    return num1+num2;
}

//add(5,8)

const addArrow =(num1:number,num2:number)=>num1+num2;

const arrr=[1,4,5];
const newArray=arrr.map((x:number)=>x*x);

const person:{
    name:string;
    balance:number;
    getBalance(money:number):number;
}={
    name:"Rasel",
    balance:56,
    getBalance(money:number){
        return this.balance+money;
    }
}

const greetingsFriends=(...fridnds:string[]):void=>fridnds.forEach((friend:string)=>console.log(`Hi ${friend} whats up`));

greetingsFriends("Rasel","Akash","Asif"); */ 
